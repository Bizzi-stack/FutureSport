# Merit Grid (Education Data Platform)
**CRITICAL AI SYSTEM NOTE**
This directory (`education-data-platform`) is the EXCLUSIVE frontend codebase for "Merit Grid". 
It operates completely independently of `classroom-main` and uses pure React state (`App.jsx`) without React Router. 

## Recent Context
- Successfully built the custom `<AdminLandingPage />` in `src/components/` and wired it into `App.jsx`.
- Integrated Hexora-style dark theme Glassmorphism.
- Implemented the Live 3D isometric `<LiveSchemaGraphic />`.
- Implemented the "Built for Barbados Secondary Schools" crests with hover-color animations.

**If the user asks to resume work on Merit Grid, YOU MUST WORK IN THIS DIRECTORY!**
